// Job Match Overview: jobs recommended with swipe actions
import { View, Text } from 'react-native';
import SwipeScreen from '../swipe';

export default function JobsScreen() {
  return <SwipeScreen />;
}
